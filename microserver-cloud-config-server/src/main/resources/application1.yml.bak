server:
  port: 8080
spring:
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/lyliu/{application}
  application:
    name: microserver-cloud-config-server

server:
  port: 8080
spring:
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/lyliu/spring-config-cloud-learning
  application:
    name: microserver-cloud-config-server